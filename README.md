# email-llm
Python script that automates email handling. The script will monitor a local directory for new email files, use a Large Language Model (LLM) via the Ollama API to understand the email's content, and send an intelligent reply.
